Expected Commands: 

3rd Tier commands (everyone):
    $judge <Img> #???#
        uses a trained neural network to rate a img containing a face through 1-10, 10 being the prettiest.

2nd Tier commands (pringleLords):
    $stalk <User>
        returns all public data relating to an individual in the server, scanning through the chat logs.

    $complete <String> <User>
        Uses an available neural network in order to complete the sentence.

    $favorite <User>
        Fetches a Users most used words, excluding 

1st Tier commands (admin):
    $train
        Trains the AI using the available chat logs
        
    $train <User>
        Trains the AI using the available chat logs from a specified user. Which later gets cached (if possible)
